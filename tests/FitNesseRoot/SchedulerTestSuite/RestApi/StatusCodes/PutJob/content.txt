'''If job does not exist then returns code 201'''
-!|script|!-RestTools-!|
|update job|${job_name_1}|uri|${request_uri}|recur|${cron_every_minute}|

|query:last response as table      |
|status_code|body.code|body.message|
|201        |201      |Job created.|

'''If job exists then returns code 200'''
-!|script|!-RestTools-!|
|update job|${job_name_1}|uri|${request_uri}|recur|${cron_every_minute}|

|query:last response as table      |
|status_code|body.code|body.message|
|200        |200      |Job updated.|

'''Query with empty id returns code 404'''
-!|script|!-RestTools-!|
|update job||uri|${request_uri}|recur|${cron_every_minute}|

|query:last response as table      |
|status_code|body.code|body.message|
|404        |=~/^$/   |=~/^$/      |

'''Query with invalid parameters returns code 400'''
-!|script|!-RestTools-!|
|update job|${job_name_2}|uri|${request_uri}|recur||

|query:last response as table               |
|status_code|body.code|body.message         |
|400        |400      |Invalid request body.|
